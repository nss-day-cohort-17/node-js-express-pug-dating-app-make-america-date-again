extends partials/layout.pug

block content
  .heading
    h1 Could You Be Loved? 🤔
  each user in likedUsersArr
    .block.col-md-6
      h3 = user.attributes.name
      h3 = user.attributes.city
      h3 = user.attributes.state
